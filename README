Program for monitoring outprint from Arduino Serial. 

It is now a very simple GUI .

TODO:
-Possible to change device. Not is uses only /dev/ttyUSB0
-Turn on/off "logging"
-Logging to file.

Add `pkg-config --libs gtk+-2.0` as a BUILD variable.